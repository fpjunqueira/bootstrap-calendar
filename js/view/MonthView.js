/**
 * Month View Object
 */
class MonthView {
    
    constructor(year, month) {
        this.fillDates(year, month);
    }

    get dates() {
        return this._dates;
    }

    // montar o componente mes 
    fillDates(year, month) {
    
    }

    fillHeader(month) {

    }

    fillDays(month) {

    } 
}